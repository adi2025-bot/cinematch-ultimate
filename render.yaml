services:
  - type: web
    name: cinematch
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mobile_server:app --workers 1 --threads 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
